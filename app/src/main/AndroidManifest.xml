<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <!-- Permisos de internet para realizar la conexion por SSH -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlphaBot2"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        <!-- FileProvider for sharing temporary crop files with UCrop -->
        <!--
            FileProvider:
            - Android no permite compartir "file://" URIs con otras actividades/apps desde Android N
              (provoca FileUriExposedException).
            - Para UCrop generamos un archivo temporal en cache y lo compartimos como "content://"
              usando un FileProvider.
            - La autoridad debe ser única: ${applicationId}.provider
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AlphaBot2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- UCrop Activity (required when manifest merger doesn't pick it up) -->
        <!--
            UCropActivity:
            - UCrop funciona como una Activity aparte (pantalla de recorte).
            - En algunos proyectos el "manifest merger" no la añade automáticamente,
              y la app crashea con ActivityNotFoundException.
            - Por eso la declaramos explícitamente aquí.
        -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>